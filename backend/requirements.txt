# --- CORE DJANGO & DATABASE ---
Django==6.0.3 // The main web framework.
dj-database-url // Connects to Render's live Postgres database.
psycopg2-binary // The engine that talks to PostgreSQL.
django-cors-headers==4.9.0 // Allows React to talk to this backend.
python-dotenv==1.2.2 // Loads your secret API keys from the .env file.

# --- AUTHENTICATION & API ---
djangorestframework==3.16.1 // Tools for building the API.
djangorestframework_simplejwt==5.5.1 // handles secure login tokens.
django-rest-passwordreset==1.5.0 // The library for the email password reset feature.

# --- GOOGLE AI & NETWORKING (Version-Free to Fix Conflicts) ---
# We remove version numbers here so pip can solve the Python 3.14 conflict automatically.
google-generativeai
google-auth
google-api-python-client
google-api-core
google-ai-generativelanguage
googleapis-common-protos
grpcio
grpcio-status
protobuf
requests==2.32.5